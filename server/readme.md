# Train Navigator Backend

This is the backend for the train connection search. The implementation is done with nodejs

## Test

Unit tests are done with mocha and chai. To run them do:

```
npm test
```
